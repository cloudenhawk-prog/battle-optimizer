Since actions can have multiple damage types, we need a good way to handle this in the character cards. We need a good way to handle this in a way that serves its purpose. Since the main purpose of seeing damage type distribution and damage dealt of different damage types is to see how efficient it is to build certain stats on them, we might want to go with the approach: If an action deals X dmg with dmg types A, B, C, then it counts as X damage towards all A, B, C damage types at once. That means the character cards don't actually show the true total damage dealt, and the total % displayed also will surpass 100 %. In return the values relative to each other are true in terms of how much each damage type is worth.


Plunge Attack (from Cartethyia) still shows up in NEGATIVE STATUSES card even though it has nothing to do with global stuff. It's HER action and the damage belogns to HER.

On-hover doesn't work for the BUFF COVERAGE section. Something must have gone very wrong since it should be extremely easy to add a hover effect to them. Like a mentioend before I want something similar to the on-click effect from the Status Tags in the rotation table.

Remove the blue scanner effect from top to bottom (animation)

How are buff coverage (uptime) calculated? Intuitively I would expect it's impossible to reach 100 % unless the modifier is active from the get-go? Unless it only counts from first application time, or..?

Eventually work further with DPS Efficiency - consider if it should be inserted into the pie diagrams by being able to switch between Raw and Time Based