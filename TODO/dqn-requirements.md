### DQN requirements

# Actions
requiredFollowUp type only allows a single action. Should allow for potentially multiple follow up actions (for example skill + liberation)

Combo system should work with BA_X combos with swaps/persistance time/follow up options - disallow BA combos that dont fit the current combo state