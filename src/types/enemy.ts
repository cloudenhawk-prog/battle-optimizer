import type { EnemyStats } from "./stats"

// ========== Type: Enemy ======================================================================================================

export type Enemy = {
  name: string
  stats: EnemyStats
}
